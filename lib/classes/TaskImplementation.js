class TaskImplementation {

    constructor() {

    }

    /**
     * Takes a Task of the type that this implementation is designed to handle and executes it.
     *
     * The function is expected to return true upon a succesful execution, or false upon a failure.
     *
     * The function is called inside an try catch so as to prevent a failure of the entire application in the event of
     *  a bad implementation.
     *
     * @this {Task} to be processed
     */
    *process() {

    }

}